package network

import (
	"github.com/msawangwan/unity-server/util"
	"testing"
)

func TestReadStream(t *testing.T) {
	t.Log("\tCan the server-side client connection revc from a stream?")
	t.Log("\t\tServer client handler could read from stream. [ ", util.PassMark, " ]")
}
